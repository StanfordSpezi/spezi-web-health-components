import { ReactNode } from 'react';
interface RowDropdownMenuProps {
    children?: ReactNode;
    itemName?: string;
}
/**
 * Standard DataTable row actions dropdown menu
 * */
export declare const RowDropdownMenu: ({ children, itemName, }: RowDropdownMenuProps) => import("react").JSX.Element;
export {};
